# Install packages needed to run the transport model scripts

install.packages("ggplot2",
                 "grid",
                 "gridExtra",
                 "deSolve",
                 "AquaEnv")
